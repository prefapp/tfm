# Comprehensive reference

[`values.reference.yaml`](./values.reference.yaml) shows multiple `applications` with `roles`, optional `scope`, and `federated_credentials`. Not applied by Terraform directly.
