# Comprehensive reference — `azure-application`

`values.reference.yaml` sketches **redirects**, **members** (object IDs), **msgraph_roles**, optional **client_secret** rotation, and **extra_role_assignments** on a subscription scope.

Replace GUIDs and scope with real values. Graph permission `id` strings must match the IDs your tenant expects for delegated or app roles.
