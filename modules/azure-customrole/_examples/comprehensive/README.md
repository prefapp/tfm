# Comprehensive reference — `azure-customrole`

`values.reference.yaml` shows multiple **assignable scopes** and a **permissions** block with `actions` and `not_actions`.

This folder is documentation-only unless you map `values` into Terraform variables for your own root module. Azure scope strings must be real subscription or management-group IDs in your environment.
