# Basic example

Runnable layout for **Standard** tier Redis with a **private endpoint**. Requires a real resource group, VNet, subnet, and private DNS zone in your subscription.

Configure the `azurerm` provider, then:

```bash
terraform init
terraform plan
```

See [`main.tf`](./main.tf).
