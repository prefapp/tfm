# Basic example — `azure-resource-group`

Creates one resource group via the module.

Set `name` and `location` to values allowed in your subscription. Configure the `azurerm` provider (subscription, auth) before `terraform apply`.
