# Basic example

Creates a user-assigned managed identity with one subscription-scope RBAC assignment. Replace **`resource_group`**, **`scope`**, and names with values in your environment before `terraform apply`.

See [`main.tf`](./main.tf).
