# Comprehensive reference

- [`module.reference.hcl`](./module.reference.hcl) — two assignments (`role_definition_name` vs `role_definition_id`), matching the historical README HCL sample.
- [`values.reference.yaml`](./values.reference.yaml) — same data as YAML for `yamldecode` or external tooling.

Keep large samples here instead of embedding them in the terraform-docs `README.md` body.
