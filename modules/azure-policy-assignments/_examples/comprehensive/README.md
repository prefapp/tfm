# Comprehensive reference

[`values.reference.yaml`](./values.reference.yaml) shows several `assignments` entries (different `scope` values). Replace resource IDs, subscription IDs, and names before use.
