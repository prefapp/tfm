# Comprehensive reference — `azure-event-hub`

`values.reference.yaml` mirrors the style of the legacy README example (namespace, IP rules, Event Grid system topic, and an event hub with subscription). Values are illustrative only.
