# Basic example — `azure-event-hub`

Minimal namespace plus one event hub, one consumer group, and one authorization rule. `system_topic` is empty so no Event Grid resources are created.

Run `terraform init` and `terraform validate` locally; configure Azure auth and replace names before `terraform apply`.
