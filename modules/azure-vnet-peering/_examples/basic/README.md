# Basic example

Calls the module with placeholder VNet and resource group names. **Create or identify** the two VNets (and their resource groups) in Azure first, then update the `module` arguments to match.

## Usage

```bash
terraform init
terraform plan
```

## Configuration

See [`main.tf`](./main.tf).
