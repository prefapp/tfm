# Comprehensive reference

[`values.reference.yaml`](./values.reference.yaml) shows both RBAC-only and access-policy shapes (do not enable both at once). Use with `yamldecode()` or copy into your root module.
