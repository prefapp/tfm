# Basic example

Terraform layout in [`main.tf`](./main.tf). Optional mirror in YAML: [`values.yaml`](./values.yaml) (for `yamldecode` or tooling). Replace **resource group**, **vault name**, and **object_id** before apply.
