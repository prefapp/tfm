# Basic example — `azure-flexible-server-postgresql`

Minimal module wiring for local `terraform init` / `terraform validate`. Configure Azure credentials and replace resource names before `terraform apply`.

You must pass **`postgresql_flexible_server_configuration`** (use `{}` if you have no extra parameters).
