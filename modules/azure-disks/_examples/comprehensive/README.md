# Comprehensive reference — `azure-disks`

`values.reference.yaml` shows a richer **`disks`** list (mixed types and sizes), optional **`tags_from_rg`**, and placeholders for **`principal_id`** when you enable **`assign_role`**.

This directory is documentation only; nothing here is executed by Terraform unless you copy values into your own root module.
