# Basic example — `azure-disks`

Creates a resource group and one managed disk through the module.

Configure the `azurerm` provider (subscription, authentication) before `terraform apply`. Adjust `name`, `location`, and disk settings for your environment.
