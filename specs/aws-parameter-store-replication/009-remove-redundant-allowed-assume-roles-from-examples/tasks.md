# Tasks: Remove redundant `allowed_assume_roles` from usage examples

**Module:** `aws-parameter-store-replication`
**Spec:** `spec.md`
**Plan:** `plan.md`

## Task List

- [x] 1. Remove redundant `allowed_assume_roles` blocks from header examples
- [x] 2. Regenerate `README.md` with `terraform-docs .`
- [x] 3. Verify generated examples

## Validation Notes

- Confirmed `README.md` no longer contains `allowed_assume_roles = [...]` blocks in usage snippets.
