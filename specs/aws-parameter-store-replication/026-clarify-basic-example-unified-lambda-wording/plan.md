# Plan: Clarify basic example wording for unified Lambda and optional EventBridge

**Spec:** `spec.md`
**Module:** `modules/aws-parameter-store-replication`

## Steps

1. Update the two bullets in `_examples/basic/README.md` under "What it creates".
2. Use precise wording for unified Lambda behavior and optional EventBridge trigger.
3. Run `terraform-docs .` in module root to keep generated docs process-compliant.
4. Validate diagnostics for changed files.
