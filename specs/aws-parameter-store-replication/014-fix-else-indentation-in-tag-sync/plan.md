# Plan: Fix else indentation in tag-sync update path

**Spec:** `spec.md`
**Module:** `modules/aws-parameter-store-replication`

## Steps

1. Correct indentation under the `else:` branch in `src/common/replication.py`.
2. Validate file diagnostics.
3. Mark tasks complete.
