# Plan: Clarify full-sync invalid flag warning wording

1. Update invalid full-sync flag warning text in `src/handler.py` to mention invalid type or value.
2. Run syntax/diagnostics checks for `handler.py`.
