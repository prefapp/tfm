# Tasks: Update addon conflict settings for aws-eks defaults

**Module:** `aws-eks`
**Spec:** `spec.md`
**Plan:** `plan.md`

## Tasks

- [x] 1. Create `spec.md`, `plan.md`, and `tasks.md` for this change.
- [x] 2. Update `modules/aws-eks/addons_locals.tf` to use `resolve_conflicts_on_create` and `resolve_conflicts_on_update`.
- [x] 3. Add `most_recent = "false"` to all base addon defaults.
- [x] 4. Run formatting/validation checks for touched module files.
- [x] 5. Final review and mark all tasks complete.

**Status:** Complete
